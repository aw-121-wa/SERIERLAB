# Serial Lab 使用说明

适用版本：0.2.32。面向安装插件的用户，无需下载插件源码或安装 Node.js。

推荐首先点击侧栏的 **首次连接向导**。选择串口时，向导依次选择端口、波特率和协议；选择 SWD 时，依次准备环境、选择探针、手动选择芯片、按需安装支持包，再选择 ELF 和监视变量。插件不会从 ELF 推断芯片型号。缺少型号/ELF 时点击 SWD 连接也会打开向导。

侧栏及工作台的 **实际连接配置与诊断** 展示当前生效值和下次连接配置；`.seriallab.json` 的串口配置优先于侧栏保存的设置，修改后需要重连。诊断区分未打开、RX 为零、收到数据但当前协议尚未解析完整采样；它不会把短暂等待或不完整帧直接判为硬件故障。**复制诊断报告** 可复制状态、配置和错误，报告包含本机路径与探针 ID。

离线电脑可使用 **导入离线环境**；在有可用环境的电脑点击 **导出离线环境** 生成 `.slrt` 包，具体平台限制及芯片包说明见 [离线环境说明](SWD_OFFLINE.md)。

Serial Lab 是 VS Code 内的串口调试和参数调整工具，提供波形、文本/HEX 终端，以及两种调参方式：

| 使用方式 | 连接方式 | 固件要求 |
| --- | --- | --- |
| Native 串口调参 | 串口 | 接入插件对应的 Native SDK/协议 |
| SWD / DAPLink 调参 | 调试探针连接 STM32 的 SWD 接口 | 带调试信息的匹配 ELF，以及可写的 RAM 参数；无需上位机通信代码 |

下面主要介绍 SWD 调参。串口连接和 SWD 连接独立，使用 SWD 不必先连接 COM 口。

## 1. 安装插件

1. 安装 VS Code 1.90 或更新版本。
2. 获取发布者提供的 `serial-lab-0.2.32.vsix` 安装包。
3. 打开 VS Code 的扩展面板，点击右上角 `…`，选择 **从 VSIX 安装… / Install from VSIX…**。
4. 选择安装包，安装完成后按提示重载窗口。也可在命令面板运行 `Developer: Reload Window`。
5. 用 VS Code 打开自己的 STM32 工程文件夹。SWD 功能需要受信任的工作区。
6. 在命令面板运行 **Serial Lab: Open Workbench**，打开工作台。

升级时同样安装新版 VSIX 并重载窗口。

## 2. 首次准备 SWD 环境

默认无需自行安装 Python 或 pyOCD。

1. 打开工作台的 **Parameters**，将“参数来源”切换为 **SWD / DAPLink**。
2. 在 VS Code 设置中搜索 `serialLab.swd.pythonPath`，保持为空。
3. 点击 **安装/修复环境**，在提示中确认安装。首次选择 ELF 或连接 SWD 时也会触发环境准备。
4. 等待插件下载并安装独立的 Python 3.11、pyOCD 和 pyelftools，完成后即可复用。

环境保存在插件的用户数据目录中，不写入 STM32 工程，也不修改系统 PATH。首次安装需要访问 GitHub、Python 下载源及 PyPI；可使用 VS Code 的 `http.proxy` 设置或 `HTTPS_PROXY` 环境变量。安装日志位于“输出”面板的 **Serial Lab SWD Setup**。

自动环境目前支持 Windows x64、macOS x64/arm64、Linux glibc x64/arm64，需要系统提供 `tar`。USB 驱动和 Linux 设备访问权限仍需按本机情况配置。

如果希望使用已有 Python，请在该解释器中安装依赖：

```powershell
python -m pip install "pyocd==0.45.1" "pyelftools==0.33"
```

然后将 `serialLab.swd.pythonPath` 设为该 Python 可执行文件的完整路径，例如 `D:/tools/python/python.exe`。不要填写 `py -3.11` 这样的命令。此设置非空时优先使用自定义环境；想恢复自动管理，请清空它。

## 3. 连接硬件

| DAPLink 接口 | STM32 端 |
| --- | --- |
| SWDIO | SWDIO |
| SWCLK | SWCLK |
| GND | GND |
| 目标电压参考接口（如有） | 按探针说明连接目标电压参考 |

确认开发板正常供电，电压与探针兼容。不要将电压参考接口直接当作供电输出使用。

STM32 应已经烧录固件并处于运行状态。关闭正在占用同一探针的 Keil 调试会话、OpenOCD 或其他调试工具。DAPLink 的虚拟串口与 SWD 是不同接口，仅选择它的 COM 口不能完成 SWD 调参。

本功能用于运行态 RAM 读写，不负责烧录，也不会替固件执行电机使能或底盘解锁。

## 4. 工程代码需要满足什么要求

- 编译生成包含 DWARF 调试信息的 ELF，例如 GCC 使用 `-g` 或 `-g3`。可以保留正常优化，但目标变量不能被优化掉。
- 选择的 ELF 必须与实际烧录的固件一致。重新编译并更换固件后，应重新选择对应文件。
- 参数必须有固定、可写的 RAM 地址，推荐使用实际参与运算的 `volatile` 全局变量或结构体成员。
- 算法需要在运行时读取这个参数；如果仅在初始化时复制一次，之后修改原变量不会自动更新算法。
- 不要选择会被传感器任务或其他代码持续覆盖的变量作为调参入口。

示例：给角度反馈增加一个可调偏移，单位为度。

```c
volatile float yaw_offset_deg = 0.0f;

/* 在原有控制循环中，读取偏移并按原算法处理角度范围。 */
float feedback_yaw = measured_yaw + yaw_offset_deg;
/* 将 feedback_yaw 交给角度环计算。 */
```

在插件中选择 `yaw_offset_deg`，写入新值后，控制循环即可读取；写入 `0` 恢复无偏移。实际生效方式取决于自己的角度环实现。

当前支持 `float32`、`int32`、`uint32`、1 字节 `bool`，以及这些类型的嵌套结构体成员和一维固定数组元素。固定数组最多 256 个元素，实际选择的监视值最多 64 个。32 位值需要 4 字节对齐。

宏、常量、临时局部变量、指针解引用、位域、union、64 位值及多维数组不属于当前支持范围。

**启用 D-Cache 的 STM32F7/H7 工程：** `volatile` 不能解决缓存一致性。调参区需要放在调试口可访问的不可缓存 RAM 中，或由固件实现正确的缓存维护；具体区域和 MPU 配置应按芯片及工程确定。

## 5. 选择芯片和参数

1. 点击 **SWD 设置**，填写 `serialLab.swd.target`。这里需要实际芯片对应的 **pyOCD target ID**，不能随意填系列名称或通用 Cortex-M。
2. 如果缺少该芯片的支持包，填写 target 后点击 **安装芯片支持**，确认下载，完成后再连接。
3. 点击 **选择 ELF**，选择本次烧录对应的 `.elf` 或包含 DWARF 的 `.axf` 文件。
4. 在列表中勾选需要调节的变量；之后也可点击 **选择参数** 调整列表。
5. 点击 **连接 SWD**。插件会核对 ELF 中的 Flash 加载内容与板上固件，不一致时拒绝连接。
6. 连接成功后查看参数值。在输入框中修改数值，按 Enter 或离开输入框提交；布尔值通过复选框修改。
7. 插件会在写入后读回核对。完成后点击 **断开 SWD**。

选择的 ELF 和参数保存在工作区设置中。连接期间更改设置后，请断开并重新连接。切换回 Native 面板不会自动断开 SWD。

对电机或底盘，建议先在停止状态完成连接和参数检查，再按自己的固件流程使能执行机构。能否写入 RAM 与底盘是否解锁是两回事。

## 6. 可选配置：限制范围与降低读取频率

可在工程的 `.vscode/settings.json` 中合并以下配置。芯片 ID、ELF 路径和变量名均需替换为自己工程的值：

```json
{
  "serialLab.swd.pythonPath": "",
  "serialLab.swd.target": "stm32f407vg",
  "serialLab.swd.elf": "build/firmware.elf",
  "serialLab.swd.frequency": 1000000,
  "serialLab.swd.pollHz": 5,
  "serialLab.swd.watch": [
    { "path": "yaw_offset_deg", "min": -10, "max": 10 }
  ]
}
```

`frequency` 是 SWD 时钟，单位 Hz；`pollHz` 是参数读取频率，默认 5 Hz，上限 20 Hz，设为 0 时仅手动刷新。`min`、`max` 是插件侧写入限制，不能替代固件自身的控制保护。

少量参数、低频读取能减少总线访问，但不保证零时序影响。多个参数逐个写入，不保证同一个控制周期同时生效。写后读回成功表示内存值一致，并不证明算法已经使用该值。

修改只保存在 RAM，复位或断电后通常恢复初始值。调好后请将参数写回源代码中的默认配置，重新编译、烧录；插件不会自动保存到 Flash。

## 7. 常见问题

| 现象 | 处理方法 |
| --- | --- |
| `No module named 'pyocd'` | 检查是否设置了自定义 `pythonPath`。清空后使用“安装/修复环境”，或给该路径对应的 Python 安装依赖。 |
| 自动安装超时或下载失败 | 查看 **Serial Lab SWD Setup** 输出，检查下载站点连通性及代理，再重试。离线电脑需要预先准备包含依赖的自定义 Python 环境。 |
| 提示没有注册 `serialLab.swd.elf` 配置 | 确认安装了支持 SWD 的新版插件，并重载 VS Code 窗口。 |
| 找不到 ELF 文件 | 使用“选择 ELF”重新选择现有文件。移动或重命名工程后，旧路径不会自动更新。 |
| 找不到目标芯片 | 检查 target ID，点击“安装芯片支持”，完成后重连。 |
| 找不到探针或连接失败 | 检查供电、SWD 接线、USB 驱动及探针占用。连接多个探针时填写 `serialLab.swd.probeId` 指定完整 ID。 |
| ELF 与固件不匹配 | 用自己的烧录工具烧录该 ELF 对应固件，再选择同一次构建的 ELF。 |
| 参数不出现在列表中 | 检查调试信息、变量类型和固定 RAM 地址，确认未被优化掉。 |
| 写入后数值马上恢复 | 检查固件是否正在覆盖变量，改用独立调参变量。 |
| 数值变了但控制无变化 | 检查算法是否持续读取该变量、执行机构是否使能，以及 D-Cache 一致性。 |
| 提示目标未运行 | 结束断点暂停，让固件继续运行后再连接；插件不会主动恢复或复位目标。 |

当前版本已完成编译和自动化测试；自动环境的真实下载验证曾受 GitHub 连接超时阻断，首次安装仍需要在使用者网络中验证。不同探针、芯片和缓存配置也需要实际硬件验证。

## 8. 串口波形与 Native 使用

普通串口使用：打开 Serial Lab 侧栏，刷新并选择端口，设置波特率，连接后选择与固件输出一致的 JustFloat、FireWater、RawData 或 Custom 协议，在工作台查看波形或终端。可通过命令面板的 **Serial Lab: Export Samples CSV** 和 **Serial Lab: Export Raw Log** 导出数据。

Native 调参需要固件接入对应 SDK/协议，选择 Native 协议并连接串口后，在 Parameters 中选择 **Native 串口**。仅定义 RAM 变量不会自动获得 Native 调参能力。

SWD 参数读取用于参数面板，不等同于高速波形采集。串口波形仍需要固件输出相应数据。

更详细的 SWD 限制和开发说明见 [SWD.md](SWD.md)。
